$(function(){
  $('.datepicker').datepicker({ dateFormat: 'yy-mm-dd'});
  $('.chosen-select').chosen({ width: '300px' });
  $('.project-detail').click(function(evt){
    evt.stopImmediatePropagation();
    href = $(this).find('a').attr('href');
    $(location).attr('href',href);
  })
})
